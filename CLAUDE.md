# re95-relay

Node.js relay backend for the **re95** anonymous imageboard PWA.

See [`.claude/CLAUDE.md`](.claude/CLAUDE.md) for full architecture, API reference, and deployment guide.

## Quick Start

```bash
cp .env.example .env   # edit as needed
npm run dev
curl http://localhost:3001/api/health
```
